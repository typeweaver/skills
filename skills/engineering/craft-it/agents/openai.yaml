interface:
  display_name: "Craft It"
  short_description: "Build the smallest complete change for an agreed outcome"
  default_prompt: "Use $craft-it to implement this change as the smallest complete solution, with tests that fail without it, and report anything else you find as follow-ups."
