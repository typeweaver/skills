interface:
  display_name: "Plan It"
  short_description: "Write the plan another agent implements from"
  default_prompt: "Use $plan-it to turn this settled approach into a plan with a runnable check for every step."
