interface:
  display_name: "Guard It"
  short_description: "Set up proven, machine-enforced guardrails for a TypeScript project"
  default_prompt: "Use $guard-it to harden this TypeScript project with strict compiler, type-aware lint, complexity, dead-code, boundary, and supply-chain checks that are proven to fire in CI."
