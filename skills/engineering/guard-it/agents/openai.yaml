interface:
  display_name: "Guard It"
  short_description: "Set up proven, machine-enforced quality gates for a TypeScript project"
  default_prompt: "Use $guard-it to harden this TypeScript project with quality gates that are proven to fail CI on real violations."
