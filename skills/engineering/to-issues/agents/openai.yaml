interface:
  display_name: "To Issues"
  short_description: "Record follow-ups locally or sync approved issues"
  default_prompt: "Use $to-issues to record these follow-ups where they belong, and publish to a tracker only if I have authorized it."
