---
"equip-it": patch
---

Revise `craft-it`: replace generic design bullets with observable tells, use one term for the agreed outcome, report pre-existing defects as follow-ups instead of fixing them unasked, and route an unexplained defect to `debug-it` and a standalone document to `document-it`.
