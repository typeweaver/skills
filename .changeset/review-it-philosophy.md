---
"equip-it": patch
---

Revise `review-it`: severities defined by diff-visible tells, findings that quote the changed line and name the failure, perspectives invoked through their `ask-*` skills, and delegation moved to the handoff reference.
