---
"equip-it": patch
---

Print expected outcomes such as conflicts, missing receipts, and adapter drift
as plain messages without a stack trace, accept `--yes` on `update` and
`uninstall` for consistency, and ship a minified bundle.
