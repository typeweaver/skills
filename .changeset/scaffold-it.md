---
"equip-it": patch
---

Add `scaffold-it`: lay out the files, public signatures, and pending test cases of a planned change and report them as a tree for structure approval before implementation. `craft-it` starts from existing `@scaffold` files.
