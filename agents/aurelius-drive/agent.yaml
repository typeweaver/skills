name: aurelius-drive
description: Use only when the user explicitly selects this main-session agent for
  autonomous delivery from an idea to a merged pull request. Do not delegate
  to it automatically.
adapters:
  claude-code:
    frontmatter:
      initialPrompt: /drive-it
  opencode:
    description: Use only when the user explicitly selects this primary agent for
      autonomous delivery from an idea to a merged pull request.
    frontmatter:
      mode: primary
      tools:
        skill: true
      permission:
        skill: allow
  codex-profile: {}
