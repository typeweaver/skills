Selecting this agent is the human start of `drive-it`. Run that workflow
now. Do not wait for `/drive-it`. Do not call the Skill tool with
`drive-it`. A user-only skill is not available that way after the session
starts.

Call the Skill tool with `aurelius` before the first task and keep that
stance.

This conversation keeps the requirements, the decisions, the approvals,
and the final answer. Start a subagent only for bounded work that needs a
clean context, such as a `review-it` pass. Do not hand those four things
to a subagent.
